# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    jwt_ttl: '%env(int:JWT_TTL)%'
    firebase_credentials: '%env(resolve:FIREBASE_CREDENTIALS)%'
    evolution_api_base_url: '%env(EVOLUTION_API_BASE_URL)%'
    evolution_api_key: '%env(EVOLUTION_API_KEY)%'
    evolution_api_instance: '%env(EVOLUTION_API_INSTANCE)%'
    evolution_api_otp_delay_ms: '%env(int:EVOLUTION_API_OTP_DELAY_MS)%'
    address_require_admin_area: '%env(bool:ADDRESS_REQUIRE_ADMIN_AREA)%'
    app_address_target_accuracy_meters: '%env(int:APP_ADDRESS_TARGET_ACCURACY_METERS)%'
    app_address_max_search_duration_seconds: '%env(int:APP_ADDRESS_MAX_SEARCH_DURATION_SECONDS)%'
    supabase_url: '%env(string:SUPABASE_URL)%'
    supabase_service_role_key: '%env(string:SUPABASE_SERVICE_ROLE_KEY)%'
    supabase_signed_url_ttl_seconds: '%env(int:SUPABASE_SIGNED_URL_TTL_SECONDS)%'
    supabase_profile_photos_bucket: '%env(string:SUPABASE_PROFILE_PHOTOS_BUCKET)%'
    supabase_identity_documents_bucket: '%env(string:SUPABASE_IDENTITY_DOCUMENTS_BUCKET)%'
    supabase_driver_licenses_bucket: '%env(string:SUPABASE_DRIVER_LICENSES_BUCKET)%'
    qr_allowed_ips: '%env(csv:QR_ALLOWED_IPS)%'
    qr_brute_force_max_attempts: '%env(int:QR_BRUTE_FORCE_MAX_ATTEMPTS)%'
    qr_brute_force_block_time: '%env(string:QR_BRUTE_FORCE_BLOCK_TIME)%'
    qr_default_max_scans: '%env(int:QR_DEFAULT_MAX_SCANS)%'
    qr_default_expires_in: '%env(string:QR_DEFAULT_EXPIRES_IN)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    App\Api\Controller\:
        resource: '../src/Api/Controller/'
        tags: ['controller.service_arguments']

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    App\Service\JwtAuthService:
        arguments:
            $tokenTtlSeconds: '%jwt_ttl%'

    App\Service\FcmPushClient:
        arguments:
            $credentialsPath: '%firebase_credentials%'

    App\Service\WhatsAppOtpClient:
        arguments:
            $baseUrl: '%evolution_api_base_url%'
            $apiKey: '%evolution_api_key%'
            $instance: '%evolution_api_instance%'
            $delayMs: '%evolution_api_otp_delay_ms%'

    App\Service\CreateConnectedUserAddressService:
        arguments:
            $requireAdminArea: '%address_require_admin_area%'

    App\Api\Controller\AppAddressConfigAction:
        arguments:
            $targetAccuracyMeters: '%app_address_target_accuracy_meters%'
            $maxSearchDurationSeconds: '%app_address_max_search_duration_seconds%'
        tags: ['controller.service_arguments']

    App\Service\Payment\StripePaymentProvider:
        arguments:
            $apiKey: '%env(STRIPE_API_KEY)%'
        tags: ['app.payment_provider']

    App\Service\Payment\OrangeMoneyPaymentProvider:
        arguments:
            $baseUrl: '%env(ORANGE_MONEY_BASE_URL)%'
            $apiKey: '%env(ORANGE_MONEY_API_KEY)%'
        tags: ['app.payment_provider']

    App\Service\Payment\MtnMoneyPaymentProvider:
        arguments:
            $baseUrl: '%env(MTN_MONEY_BASE_URL)%'
            $apiKey: '%env(MTN_MONEY_API_KEY)%'
        tags: ['app.payment_provider']

    App\Service\PaymentService:
        arguments:
            $providers: !tagged_iterator app.payment_provider

    App\Service\SupabaseStorageClient:
        arguments:
            $baseUrl: '%supabase_url%'
            $serviceRoleKey: '%supabase_service_role_key%'
            $signedUrlTtlSeconds: '%supabase_signed_url_ttl_seconds%'

    App\Service\ProfilePhotoStorage:
        arguments:
            $storage: '@App\Service\SupabaseStorageClient'
            $bucket: '%supabase_profile_photos_bucket%'

    App\Service\AccountDocumentStorage:
        arguments:
            $storage: '@App\Service\SupabaseStorageClient'
            $identityBucket: '%supabase_identity_documents_bucket%'
            $driverLicenseBucket: '%supabase_driver_licenses_bucket%'

    App\Service\QrCodeAccessPolicy:
        arguments:
            $allowedIps: '%qr_allowed_ips%'

    App\Service\QrCodeBruteForceGuard:
        arguments:
            $cache: '@cache.app'
            $maxAttempts: '%qr_brute_force_max_attempts%'
            $blockTime: '%qr_brute_force_block_time%'

    App\Controller\QrCodeController:
        arguments:
            $qrRateLimiter: '@limiter.qr_scan'

    App\Controller\GenerateQrCodeController:
        arguments:
            $qrGenerateRateLimiter: '@limiter.qr_generate'

    App\Service\AddressQrCodeService:
        arguments:
            $defaultMaxScans: '%qr_default_max_scans%'
            $defaultExpiresIn: '%qr_default_expires_in%'
